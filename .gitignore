# Dependencias
node_modules/

# Base de datos generada (se recrea al generar zonas)
server/zones.db
server/zones.db-journal

# Estado del jugador (persistencia local)
server/savegame.json
server/saves/
server/session.json

# Logs y temporales
*.log
npm-debug.log*
.DS_Store
